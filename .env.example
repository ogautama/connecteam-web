# Neon Postgres connection string (create a project at neon.tech).
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Auth.js v5 signing secret — generate with `openssl rand -base64 32`.
AUTH_SECRET=""
